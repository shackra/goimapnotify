{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch goimapnotify for debugging",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}",
      "args": ["-conf", "./local.conf", "-debug"]
    }
  ]
}
